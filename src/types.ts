export type Player = {
  id: number;
  name: string;
  position: number[];
  rotation: number[];
};
export type ClientPlayer = {
  name: string;
};
export type Position = {
  position: number[];
};
export type Rotation = {
  rotation: number[];
};
