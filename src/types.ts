export type Player = {
  id: string;
  name: string;
  position: number[];
  rotation: number[];
};
export type ClientPlayer = {
  name: string;
};
export type Position = {
  position: number[];
};
export type Rotation = {
  rotation: number[];
};
